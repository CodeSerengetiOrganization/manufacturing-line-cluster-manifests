apiVersion: v1
kind: Service
metadata:
  name: mms-backend-service
spec:
  type: NodePort
  selector:
    app: mms-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080   # exposed port on your laptop
