# THE "BUNDLE" VERSION (Calendar Versioning)
# Update this whenever you do a coordinated deployment of multiple services.
version: 2025.12.26.01 

metadata:
  updated_by: "Johnathan W"
  reason: "initial deployment"
  status: "DEV_PASSED"

# COMPONENT VERSIONS
# These are the specific tags that should be deployed.Component keys match the Kubernetes deployment names in base/
components:
  mms-backend: "v1.0.0"      # Spring Boot (deployment: mms-backend)
  mms-frontend: "v1.1.0"     # Angular (deployment: mms-frontend)
  mysql: "mysql-8.0.33"      # Infrastructure (deployment: mysql)
  kafka: "3.5.1"             # Infrastructure